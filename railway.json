{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "DATABASE_URL=$DATABASE_PUBLIC_URL npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}